name: TruffleHog Single Repo Scan

on:
  workflow_dispatch:

jobs:
  scan-single-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Clone target repo
        run: |
          git clone https://x-access-token:${{ secrets.TRUFFLEHOG_GITHUB_TOKEN }}@github.com/sshelat/Crazy_parser.git target-repo

      - name: Run TruffleHog on cloned repo
        run: |
          docker run --rm \
            -v "$PWD/target-repo:/repo" \
            ghcr.io/trufflesecurity/trufflehog:latest \
            git file:///repo \
            --results=verified,unknown \
            --json > trufflehog-results.json

          echo "Result file size:"
          wc -c trufflehog-results.json

          echo "Preview:"
          head -n 20 trufflehog-results.json || true
        continue-on-error: true

      - name: Upload TruffleHog results
        uses: actions/upload-artifact@v4
        with:
          name: trufflehog-results
          path: trufflehog-results.json
